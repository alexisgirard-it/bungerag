# Phase 1 - extraction
pymupdf==1.28.0
pymupdf4llm==1.28.0
ebooklib
beautifulsoup4
ocrmac          # OCR Apple Vision (macOS uniquement, utilise par ocr_scan.py)

# Les phases suivantes ajouteront leurs dependances ici, versions epinglees.
# Deja tranche : ragas==0.4.3 (phase 6 - NE PAS deverrouiller, la 1.0 casse l'API)

# Phase 2 - chunking
llama-index-core==0.14.23
tiktoken==0.13.0

# Phase 3 - embeddings + index
sentence-transformers==5.6.0
torch==2.12.1
lancedb==0.34.0

# Phase 5 - generation
google-genai==2.10.0
python-dotenv==1.2.2
